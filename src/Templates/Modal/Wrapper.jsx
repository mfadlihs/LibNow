import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const Wrapper = styled(Box)({
	backgroundColor: '#fff',
	minHeight: 575,
	padding: '50px 30px',
	width: 392,
	borderRadius: 20,
});
